---
paths:
  - "apps/web/**"
---

# 前端规则

- 前端不决定测评流程，只根据后端返回的状态和步骤渲染
- 前端不承担A结论的安全隐藏；服务端必须先过滤
- 考官模式与工具模式必须有明显视觉区别
- 模式切换卡片必须与AI消息明显不同
- 切换阶段后不展示上一阶段对话
- 倒计时以服务端时间数据为准，刷新不得重置
- 工具模式支持主动完成任务；考官模式不提供跳过按钮
- 不增加文件上传、音频、移动端适配或复杂设计系统

不要假设具体UI库。以架构文档、ADR、`package.json`和现有代码为准。
