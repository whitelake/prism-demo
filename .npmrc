# pnpm v11 默认 strict-build-scripts=true 会拒绝运行 @nestjs/core/esbuild 的 build scripts
# 导致 ERR_PNPM_IGNORED_BUILDS 中断 install。
# CI/Docker 场景下用 dangerously-allow-all-builds 放开（onlyBuiltDependencies 仍作白名单兜底）
dangerously-allow-all-builds=true
